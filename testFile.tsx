import {FC} from "react";

interface FileNameWithoutExtensionProps {
  prop: string;
}

export const FileNameWithoutExtension: FC<FileNameWithoutExtensionProps> = (props) => {
  const { prop } = props;

  return (
    <div className={``}>
      
    </div>
  );
};